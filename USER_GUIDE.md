# LuatOS Air780E 项目入门指南

欢迎！这份指南是为了帮助初学者理解我们刚刚搭建的代码结构和 Lua 的基本语法。

## 1. 项目文件结构 (Project Structure)

为了不让代码乱成一团，我们将功能分成了几个小文件（模块）：

| 文件名 | 作用 | 比喻 |
| :--- | :--- | :--- |
| **`main.lua`** | **入口文件** | **总指挥**。板子一通电，先从这里开始运行。它负责加载其他模块并启动系统。 |
| **`usr_config.lua`** | **配置文件** | **记事本**。记录了所有的设置（比如灯接在几号脚，多久报一次）。要改参数就来这，别乱动代码。 |
| **`usr_led.lua`** | **LED驱动** | **灯光师**。它只管怎么开灯、关灯。别人想开关灯，直接找它，不用管底层电路。 |
| **`usr_lbs.lua`** | **定位服务** | **导航员**。它负责和基站通讯，算出经纬度。 |
| **`usr_app.lua`** | **主业务逻辑** | **剧本**。它规定了整个流程：醒来 -> 叫灯光师开灯 -> 问导航员位置 -> 关灯 -> 睡觉。 |

---

## 2. Lua 基础语法 (Basic Syntax)

我们在代码中用到了一些核心概念，这里简单解释一下：

### 2.1 引入模块 (`require`)
就像是“呼叫队友”。
```lua
local sys = require("sys")          -- 呼叫系统库，起名叫 sys
local config = require("usr_config") -- 呼叫我们写的配置文件，起名叫 config
```
*   **注意**：`require` 只要写文件名（不带 `.lua`）。

### 2.2 局部变量 (`local`)
Lua 里的变量默认是全局的（到处都能由），但这很危险。
**习惯**：永远用 `local` 定义变量，表示“这个变量只在我当前的文件/函数里有效”。
```lua
local LED_PIN = 27 -- 这个 LED_PIN 只有在这里能用，别的代码看不见，就不会冲突
```

### 2.3如果不写 local (`全局变量`)
`PROJECT` 和 `VERSION` 我们没写 `local`，因为 LuatOS 系统需要知道它们，所以必须是全局的。
```lua
PROJECT = "TRACKER_PRO" -- 全局皆知
```

### 2.4 表 (`table`)
表是 Lua 唯一的复杂数据结构，你可以把它看作一个“收纳盒”。
```lua
local led = {}  -- 创建一个空的收纳盒，名字叫 led

function led.on() -- 在 led 盒子里放一个工具（函数），叫 on
    -- 开灯的代码
end

return led -- 把这个盒子交出去，别人 require 的时候我就给他这个盒子
```
所以在别的代码里，我们就能用 `led.on()` 来调用这个功能。

### 2.5 任务 (`sys.taskInit`)
LuatOS 是多任务系统。`sys.taskInit` 就像是“新建一个线程”或者“雇佣一个工人”。
```lua
sys.taskInit(function()
    while true do
        -- 这里面的代码会一直循环运行
        -- 就像工人在不停地干活
        sys.wait(1000) -- 工人休息 1 秒
    end
end)
```

## 4. 核心库详解 (`sys`)

**`sys`** 是 LuatOS 的心脏，负责管理时间和任务。

*   **`sys.wait(ms)`**: **睡觉**。让当前任务暂停一段时间。这是省电的关键！
    *   *注意*：它只能在 `sys.taskInit` 里面用。
*   **`sys.taskInit(func)`**: **招人**。启动一个新的并行任务。
*   **`sys.run()`**: **开工**。这行代码必须放在 `main.lua` 的最后。它一运行，所有的任务才真正开始跑。
*   **`sys.publish(...)` / `sys.subscribe(...)`**: **广播/收听**。
    *   比如定位成功了，`usr_lbs` 可以喊一声 "LBS_DONE"。
    *   `usr_app` 听到这个声音，就知道定位好了。这是模块间通讯的神器。

---

## 5. 网络连接说明 (Network)
LuatOS 的网络连接全是自动的（开机即连）。

### 3.1 什么是“连上网了”？
在 4G 模组的世界里，只要**获取到了 IP 地址**，就代表连上互联网了。
*   **流程**：开机 -> 检卡 -> 注册基站 -> 拨号成功 -> **分配 IP**。
*   **代码体现**：
    *   系统消息：`IP_READY`。
    *   查询函数：`socket.localIP()` 如果不为 `0.0.0.0`，就是连上了。

### 3.2 低功耗下的网络
*   **PSM 模式**：休眠时网络是**断开**的（为了省电）。
*   **唤醒后**：模组会极速重连（通常几百毫秒），重新拿到 IP 后，我们才开始定位。
*   *代码逻辑*：`usr_app.lua` 里写了 `if socket.localIP() ...` 就是在确认这件事。

## 6. 常见问题
*   **为什么 `pm`, `sys`, `mobile` 不用 `require`?**
    *   **核心库 (Core Libs)**：在最新的 LuatOS 版本中，**`sys`** 以及 `pm`, `mobile`, `gpio` 等 14 个常用库已经被集成到内核中，**变成了全局变量**。
    *   这意味着你不需要再写 `require("sys")`，直接用 `sys.publish()` 即可。
    *   *只有我们自己写的 `usr_xxx` 模块，或者一些不常用的扩展库，才需要 `require`。*

*   **为什么报错 `module not found`?**
    通常是因为你 `require("xxx")` 时，文件名写错了，或者文件没有烧录进去。我们加了 `usr_` 前缀后，名字变得很独特，不容易搞混。

*   **`sys.wait` 和 `mobile.reqCellInfo` 有什么区别？**
    *   `sys.wait` 是**死等**（睡觉），为了省电。
## 7. 硬件集成指南 (Hardware Integration)

### 7.1 自动唤醒 (Timer)
目前的程序设置了 `pm.dtimerStart`，模组会**每隔 X 分钟自动醒来**，干活，然后继续睡。
*   此过程**不需要**任何外部干预。

### 7.2 手动/单片机唤醒 (PWRKEY)
如果您希望在休眠期间，**随时**把模组叫醒（例如：单片机突然想发数据）：
*   **方法**：拉低 **PWRKEY (第1脚)** 约 1.5秒。
    *   *为什么是 1.5秒？*：这是 **《Air780E 硬件设计手册》** 第 3.2 章节（开关机）规定的。
    *   手册要求：开机需拉低 **>1.2秒**；关机需拉低 **>1.5秒**。
    *   *注意*：如果是从 **PSM 模式** 唤醒，可能需要拉低更长时间（**建议 > 2秒**），因为模组需要从深度睡眠中恢复。
    *   为了稳妥起见，您可以尝试拉低 **2秒 ~ 2.5秒**。
    *   *注意*：虽然 **RESET 脚** 只要拉低 0.1秒 也能让模组重启（硬复位），但官方推荐尽量用 PWRKEY 进行“软重启”，这样模组有机会保存网络数据，下次联网更快。RESET 仅建议在死机时作为“急救”手段。
*   **效果**：模组会立即重启/唤醒。
*   **单片机操作**：单片机的 GPIO 连接模组的 PWRKEY，需要唤醒时输出低电平脉冲即可。不需要一直按着。

### 7.3 串口通讯 (UART)
*   **TX (Pin 28)** -> 单片机 RX
*   **RX (Pin 27)** -> 单片机 TX
*   **GND** -> 单片机 GND (必须共地！)
